#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a,b,c;
    while(scanf("%d %d %d", &a,&b,&c)>0){

        if(a>b && a>c && a!=b,){
            printf("%c",a);
        }
    }
    return 0;
}
