#include <stdio.h>
#include <stdlib.h>

int main()
{
    int x,lider;
    int y, retardario;
    scanf("%d",&x);
    scanf("%d",&y);
    if(1 <= X < Y <= 10000){
        while(x )

    }
    printf("Hello world!\n");
    return 0;
}
