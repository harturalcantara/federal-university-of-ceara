package aps_questao5;

public class Cliente {
	public static void main(String[] args) throws Exception{
		Soma opr = new Proxy();
		opr.soma(5,7);
	}
	
	/** Padrão de projeto Proxy
	 * Seguindo o mesmo modelo passado como exemplo, foi aplicado o padrão de projeto Proxy
	 * sobre a opeção de soma, onde instaciado o tipo proxy, a operacao soma pode ser chama-
	 * da dado os valores nos parametros, para imprimir a soma desses valores. 
	 * */	
}
